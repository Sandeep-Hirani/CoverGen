# Copy this file to .env and fill in the appropriate values.

# Which LLM provider to use: "openai" or "together".
LLM_PROVIDER=openai

# API keys for the LLM providers (set at least the one you plan to use).
OPENAI_API_KEY=your-openai-api-key
TOGETHER_API_KEY=your-together-api-key

# Model configuration
LLM_MODEL=gpt-4-turbo
LLM_TEMPERATURE=0.2

# Paths for resume, template, and output artifacts
CV_PATH=data/cv.txt
LATEX_TEMPLATE=templates/cover_letter.tex.j2
LATEX_ENGINE=xelatex
OUTPUT_DIR=output

# Identity defaults used by `covergen generate` when flags are omitted.
DEFAULT_SENDER_NAME=Jane Doe
DEFAULT_SENDER_ADDRESS=123 Main Street|San Francisco, CA

# Recipient name and company are derived automatically from the job info; provide CLI overrides as needed.

# Optional phrasing defaults
DEFAULT_OPENING=Dear Hiring Manager
DEFAULT_CLOSING=Sincerely,
DEFAULT_TONE=professional
